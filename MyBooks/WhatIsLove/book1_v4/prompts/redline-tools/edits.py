# Example. Each edit: (group heading shown in the redline, source line number, exact old text, new text).
# Every old text must occur exactly once in the chapter. Order matters when one edit's text overlaps another's.
E=[("Example group",42,"old sentence exactly as it appears.","new sentence.")]
